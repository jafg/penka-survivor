/**
 * Placeholder export so the package graph is wired end to end.
 * Real Survivor rules (pick validation, elimination, matchday resolution)
 * land here — and only here — behind failing tests first.
 */
export const ENGINE_NAME = '@penka/game-engine';

export function engineReady(): boolean {
  return true;
}
